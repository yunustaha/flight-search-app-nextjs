export interface Flight {
    id: string
    airline: string
    departure: string
    destination: string
    departure_time: string
    arrival_time: string
    price: number
    available_seats: number
}